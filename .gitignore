node_modules/
dist/
.next/
out/
cache/
broadcast/
packages/contracts/out/
packages/contracts/cache/
.env
.env.local
*.log
.DS_Store

# stale git temp files this cloud session could not unlink — safe to rm -rf
_to_delete/
*.tsbuildinfo

# Operational state written by apps/api (Claude session ids, and dev-only agent keys when
# WALLET_PROVIDER=local). Never commit it — it can contain private keys.
.roster-store.json
